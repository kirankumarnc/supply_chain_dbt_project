# supply_chain_dbt_project
End-to-end supply chain data pipeline using Snowflake, dbt, and AWS S3 — covering ingestion, incremental transformation, SCD Type 2 snapshots, data quality testing, and a dimensional model (Bronze → Silver → Gold).
